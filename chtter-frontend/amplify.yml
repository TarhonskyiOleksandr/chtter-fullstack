version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd chtter-frontend
        - yarn install
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: /build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/* 
