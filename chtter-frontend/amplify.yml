version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd chtter-frontend
        - yarn install
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: chtter-frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/* 
